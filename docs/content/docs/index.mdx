---
title: Welcome
description: A polyglot library for sending and receiving webhooks reliably and securely. It does two halves — inbound and outbound — and you can use either alone.
---

import { CompassIcon, RocketIcon, BookIcon, GlobeIcon } from "@/components/icons";

Sending and receiving webhooks is easy. Doing it **reliably and securely** is hard — retries, replay, signature verification, key rotation, idempotency, raw-bytes preservation. That's where Postel comes in: a polyglot library that handles those for you. It runs inside your application against your existing relational database (Postgres, MySQL, SQLite, …) — no Redis, no broker, no separate dispatcher process.

> Svix is for when webhooks are your **product**.
> Postel is for when webhooks are a **feature** of your product.

## Two halves, one library

Postel has two independent halves. Use either alone, or both together.

| Half | What it does | Status |
|---|---|---|
| **[Inbound](/docs/inbound)** | Verify Standard Webhooks-signed requests other services send to you. Constant-time signature checks, multi-secret rotation windows, JWKS, idempotent dedup, raw-bytes preservation. | **Available** in `@postel/core`. |
| **[Outbound](/docs/outbound)** | Deliver Standard Webhooks to your customers' endpoints. Transactional outbox, retries, replay, fanout, key rotation, JWKS publication, circuit breaker, dead-letter. | **Available** against the in-process `InMemoryStorage` adapter (database adapters planned). |

The `Postel({ inbound, outbound })` factory composes both — but everywhere else in these docs they're separated. You shouldn't have to wade through outbound material to integrate the receiver, or vice versa.

## Picking a starting point

<Cards>
  <Card icon={<CompassIcon />} title="Is Postel for me?" href="/docs/get-started/is-postel-for-me" description="A six-line decision gate. Run it before you read more." />
  <Card icon={<RocketIcon />} title="Quickstart" href="/docs/get-started/quickstart" description="60 seconds — verify your first webhook. TypeScript, Hono or framework-agnostic." />
  <Card icon={<BookIcon />} title="Why Postel" href="/docs/get-started/why" description="The tradeoffs; comparison vs Svix, comparison vs hand-rolled." />
  <Card icon={<GlobeIcon />} title="Polyglot" href="/docs/get-started/polyglot" description="For port authors — what conformance means and where the suite lives." />
</Cards>

## Pre-1.0 reality check

Postel is pre-alpha. The receiver is feature-complete against its capability specs, but no `@postel/*` package has cut a numbered release yet — they all sit at `0.0.0` in the workspace. The receiver works; you can clone and use it. We don't recommend production deployment until a tagged release ships on npm.

The sender runtime is available too — `send`, the transactional outbox, retries, replay, fanout, and signing all run against the in-process `InMemoryStorage` adapter. Database-backed storage adapters are planned; until they land, bring the in-memory adapter or implement the `Storage` interface against your own database. Pages under [Outbound](/docs/outbound) note what's available now versus planned.

This documentation site is the public surface of that work. If something here suggests behavior that isn't in the source — call it out in an issue.
