import type { Keyset, KeysetOptions } from "./types.js";

export function createKeyset(_options: KeysetOptions): Keyset {
  throw new Error("@postel/edge: createKeyset is not implemented in the v0.1.0 skeleton");
}
